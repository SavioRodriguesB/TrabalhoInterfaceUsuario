<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.math.calculadora">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Calculadora">
        <activity android:name=".CalculoJurosCompostoParcial"></activity>
        <activity
            android:name=".DescontoSimples"
            android:label="Cálculo Desconto Simples"
            android:theme="@style/Theme.Calculadora.NoActionBar" />
        <activity
            android:name=".CalculoJurosComposto"
            android:label="Cálculo Juros Composto"
            android:theme="@style/Theme.Calculadora.NoActionBar" />
        <activity
            android:name=".CalculoJurosSimples"
            android:label="@string/title_activity_calculo_juros_simples"
            android:theme="@style/Theme.Calculadora.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>